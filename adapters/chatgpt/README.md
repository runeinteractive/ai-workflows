# ChatGPT

```bash
./scripts/install.sh /path/to/project chatgpt
```

Paste generated `CHATGPT.ai-workflows.md` into project instructions.
Attach the matching `.ai-workflows/prompts/*.md` when running a workflow.
