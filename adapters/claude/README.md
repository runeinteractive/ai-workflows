# Claude Code

```bash
./scripts/install.sh /path/to/project claude
```

Merge generated `CLAUDE.ai-workflows.md` into the project `CLAUDE.md`.
Workflows are listed from `adapters/cursor/skills/` and point at `.ai-workflows/prompts/`.
